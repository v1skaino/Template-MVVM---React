import React from 'react';
import ReactDOM from 'react-dom/client';
import Boiderplate_APP from './__app.js';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Boiderplate_APP />
  </React.StrictMode>,
);
